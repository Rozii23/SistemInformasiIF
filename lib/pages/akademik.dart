import 'package:flutter/material.dart';

class AkademikPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman Akademik'),
      ),
      body: Center(
        child: Text('Informasi Akademik'),
      ),
    );
  }
}